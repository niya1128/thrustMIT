int pin=A3;
float V2=0;
int readVal;
int dtime=500;
void setup()
{
  pinMode(pin, INPUT);
  Serial.begin(9600);
}

void loop()
{
readVal=analogRead(pin);
V2=(5./1023.)*readVal;
Serial.println(V2);
delay(dtime);
}
